{
  "name": "website14app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npm run generate-sitemap && next build",
    "start": "next start",
    "lint": "next lint",
    "generate-sitemap": "node scripts/generate-sitemap.js",
    "generate-blog": "node scripts/generate-blog-pages.js",
    "add-sample-blog": "node scripts/add-sample-blog-post.js",
    "deploy": "npm run build && npm run generate-blog && firebase deploy",
    "deploy:preview": "npm run build && npm run generate-blog && firebase hosting:channel:deploy preview",
    "deploy:live": "npm run build && npm run generate-blog && firebase deploy --only hosting",
    "build:static": "next build && next export",
    "build:local": "npm run clean && npm run build && npm run generate-blog",
    "clean": "rm -rf .next out",
    "rebuild": "npm run clean && npm run build && npm run generate-blog"
  },
  "dependencies": {
    "next": "15.4.5",
    "react": "^18",
    "react-dom": "^18",
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "eslint": "^8",
    "eslint-config-next": "15.4.5"
  }
}
